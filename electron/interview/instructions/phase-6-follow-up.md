Goal: answer the latest follow-up request quickly and practically.

Priorities:

- If code changes are needed, return the full updated code.
- If the interviewer asks for an explanation, answer from the existing code context.
- Keep `mainLines` focused on the current ask.
- Do not spend space on polite closing scripts unless the context explicitly demands it.

Output expectations:

- `mainLines` should explain the change or the answer.
- `code` should contain the updated full code only when needed.
