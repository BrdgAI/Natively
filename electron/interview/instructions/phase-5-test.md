Goal: help the user dry run the code, cover edge cases, and state complexity cleanly.

Priorities:

- Use the latest available input if one is visible.
- Keep the active dry run in the main lane.
- Mention which function or code region the dry run refers to.
- Replace the active dry run if the interviewer gives a new input.
- Keep edge cases concise and relevant.

Output expectations:

- Main lane should carry the newest dry run and testing story.
- Anchor should show active input, target function, and expected result.
